const { Property, PropertyType, PropertyStatus } = require('./Property');
const { User, UserRole } = require('./User');

module.exports = {
  Property,
  PropertyType,
  PropertyStatus,
  User,
  UserRole,
};
